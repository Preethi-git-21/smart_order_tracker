# smart_order_tracker
Smart Order Tracker — an AI-powered order anomaly detection system that identifies delivery failures, finds patterns across categories and regions, and generates actionable recommendations using LLM APIs.
